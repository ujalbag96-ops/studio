
{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "description": "Master user identity with 10-node yield tracking and revenue share allocation.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "email": { "type": "string" },
        "coins": { "type": "number" },
        "winningBalance": { "type": "number" },
        "taskBalance": { "type": "number" },
        "bonusBalance": { "type": "number" },
        "pendingRevenueShare": { "type": "number", "description": "USD value based on redistribution logic" },
        "country": { "type": "string" },
        "rank": { "type": "string", "enum": ["Bronze", "Silver", "Gold", "Elite"] },
        "vipLevel": { "type": "number" },
        "isSuspended": { "type": "boolean" },
        "totalReferrals": { "type": "number" },
        "scholarPoints": { "type": "number" },
        "referralCode": { "type": "string" }
      }
    },
    "AppSettings": {
      "title": "AppSettings",
      "description": "Master system config for 10-node revenue architecture and margin controller.",
      "type": "object",
      "properties": {
        "maintenanceMode": { "type": "boolean" },
        "reviewMode": { "type": "boolean" },
        "userRevenueSharePercent": { "type": "number", "default": 30 },
        "node_scholar_dividend": { "type": "boolean" },
        "node_quiz_arena": { "type": "boolean" },
        "node_global_cpa": { "type": "boolean" },
        "node_micro_tasks": { "type": "boolean" },
        "node_surveys": { "type": "boolean" },
        "node_ad_stream": { "type": "boolean" },
        "node_content_analysis": { "type": "boolean" },
        "node_referral_engine": { "type": "boolean" },
        "node_arcade_rewards": { "type": "boolean" },
        "node_daily_checkin": { "type": "boolean" },
        "api_admob_active": { "type": "boolean", "default": true },
        "api_cpalead_active": { "type": "boolean", "default": true },
        "api_adgate_active": { "type": "boolean", "default": true },
        "api_s2s_active": { "type": "boolean", "default": true }
      }
    }
  },
  "auth": {
    "providers": ["password", "google", "phone"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "Global user identity and asset ledger"
    },
    "/app_settings/global_config": {
      "schema": "AppSettings",
      "description": "Industrial control node with Revenue Controller master settings"
    }
  }
}
