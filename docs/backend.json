
{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "description": "Master user identity with 10-node yield tracking and revenue share allocation.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "email": { "type": "string" },
        "coins": { "type": "number" },
        "winningBalance": { "type": "number" },
        "taskBalance": { "type": "number" },
        "bonusBalance": { "type": "number" },
        "depositBalance": { "type": "number" },
        "walletBalanceINR": { "type": "number" },
        "pendingRevenueShare": { "type": "number" },
        "country": { "type": "string" },
        "geo_region": { "type": "string" },
        "rank": { "type": "string", "enum": ["Bronze", "Silver", "Gold", "Elite"] },
        "vipLevel": { "type": "number" },
        "isSuspended": { "type": "boolean" },
        "riskNoticeAccepted": { "type": "boolean" },
        "totalReferrals": { "type": "number" },
        "cpaTasksCount": { "type": "number" },
        "generalTasksCount": { "type": "number" },
        "scholarPoints": { "type": "number" },
        "referralCode": { "type": "string" },
        "preferredLanguage": { "type": "string" }
      }
    },
    "AppSettings": {
      "title": "AppSettings",
      "description": "Master system config for 10-node revenue architecture and visibility controller.",
      "type": "object",
      "properties": {
        "maintenanceMode": { "type": "boolean" },
        "reviewMode": { "type": "boolean" },
        "autoWithdrawalEnabled": { "type": "boolean" },
        "razorpayAutoPayout": { "type": "boolean" },
        "userRevenueSharePercent": { "type": "number", "default": 30 },
        "api_razorpay_active": { "type": "boolean" },
        "api_admob_active": { "type": "boolean" },
        "api_cpalead_active": { "type": "boolean" },
        "api_s2s_active": { "type": "boolean" },
        "node_scholar_dividend": { "type": "boolean", "description": "Library & Books visibility" },
        "node_quiz_arena": { "type": "boolean", "description": "Quiz Arena visibility" },
        "node_global_cpa": { "type": "boolean", "description": "Pocket Money / CPA Tasks visibility" },
        "node_referral_engine": { "type": "boolean", "description": "Refer & Earn visibility" },
        "node_ad_stream": { "type": "boolean", "description": "Video Ads visibility" },
        "node_book_download": { "type": "boolean", "description": "Book Download button visibility" },
        "node_tutor_visible": { "type": "boolean", "description": "AI Human Tutor visibility" },
        "node_daily_streak_visible": { "type": "boolean", "description": "Daily Streak visibility" }
      }
    }
  },
  "auth": {
    "providers": ["password", "google", "phone"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "Global user identity and asset ledger"
    },
    "/app_settings/global_config": {
      "schema": "AppSettings",
      "description": "Industrial control node with Revenue Controller master settings"
    }
  }
}
