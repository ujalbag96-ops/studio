{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "description": "User profile with academic focus and earnings.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "email": { "type": "string" },
        "coins": { "type": "number" },
        "department": { "type": "string" },
        "semester": { "type": "number" },
        "riskNoticeAccepted": { "type": "boolean" }
      }
    },
    "StudyMaterial": {
      "title": "StudyMaterial",
      "description": "Educational resources for students",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "department": { "type": "string" },
        "semester": { "type": "number" },
        "type": { "type": "string", "enum": ["Notes", "PYQ", "Syllabus"] },
        "url": { "type": "string" },
        "isPremium": { "type": "boolean" }
      }
    }
  },
  "auth": {
    "providers": ["password", "google", "phone"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "User account data"
    },
    "/study_materials/{materialId}": {
      "schema": "StudyMaterial",
      "description": "Library of notes and papers"
    }
  }
}